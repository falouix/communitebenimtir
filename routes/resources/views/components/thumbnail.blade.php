<div class="caption">
    <a href="{{$public_url}}{{ $link }}" target="{{ $target }}">
        <h5>{{ $slot }}</h5>
    </a>
    <span><img data-src="{{ voyager::image($icon) }}" class="lazyload"></span>
</div>